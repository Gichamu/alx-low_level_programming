#include <stdio.h>

/**
 * main - entry point
 *
 * Return: always 0 (sucess)
 */
int main(void)
{
	char a = 'a';
	char z;

	while (a <= z)
	{
		putchar('a');
		a++;
	}
	putchar('\n');
	return (0);
}
