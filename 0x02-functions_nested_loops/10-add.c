#include "main.h"

/**
*add - adds two integers and returns results
*@a: the first integer
*@b: the second integer
*Return: sum of two integers
*/
int add(int, int);
{
	int a, b;
	sum = a + b
	return (sum);
}
