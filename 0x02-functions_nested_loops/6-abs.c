#include "main.h"

/**
*_abs - prints absolute value of integer
*@i: function parameter
*Return: computes value of integer
*/
int _abs(int i)
{
	if (i > 0)
	return (i);
	else if (i == 0)
	return (0);
	else
	return (-(i));
}
