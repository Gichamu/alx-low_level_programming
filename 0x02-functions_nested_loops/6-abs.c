#include "main.h"

/**
*_abs - computes absolute value of an integer
*@n: integer value it takes as argument
*Return: 0
*/
int _abs(int)
{
	if (n > 48)
	{
		_putchar(n);
	}
	else if (n == 0)
	{
		_putchar(n);
	}
	else
	{
		_putchar(n);
	}
	_putchar('\n');
}
