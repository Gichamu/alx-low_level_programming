#include "main.h"

/**
*_abs - computes absolute value of an integer
*@int: data type and integer it takes as argument
*Return: 0
*/
int _abs(int i)
{
	if (i > 48)
	{
		_putchar(i);
	}
	else if (i == 0)
	{
		_putchar(i);
	}
	else
	{
		_putchar(i);
	}
	_putchar('\n');
}
